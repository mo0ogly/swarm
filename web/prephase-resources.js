const tr_web_prephase_resources_js = source => globalThis.SwarmI18n?.t(source) ?? source;
// Explicit, versioned excerpts and estimate envelopes; no autonomous file access.
export class PreparationResources {
 constructor(api,current,apply){this.api=api;this.current=current;this.apply=apply;this.p=null;this.pending=null;this.busy=false;this.preview=null;this.generation=0;this.$=id=>document.getElementById(id);
  const on=(id,event,fn)=>this.$(id).addEventListener(event,e=>{if(e.isTrusted)fn(e)});
  on('sources-open','click',()=>this.open('sources'));on('budget-open','click',()=>this.open('budget'));
  on('source-search-form','submit',e=>{e.preventDefault();this.search()});on('source-read','click',()=>this.read());on('source-add','click',()=>this.add());on('budget-form','submit',e=>{e.preventDefault();this.saveBudget()});
  for(const id of ['source-path','source-start','source-end'])on(id,'input',()=>{this.generation++;this.preview=null;this.$('source-add').disabled=true;});
  for(const kind of ['sources','budget']){this.$(kind+'-dialog').addEventListener('close',()=>this.$(kind+'-open').focus());this.$(kind+'-dialog').addEventListener('cancel',e=>{if(this.busy||this.pending)e.preventDefault()})}
 }
 hasPending(){return this.busy||!!this.pending;}
 paint(p){if(this.p?.id!==p.id){this.generation++;this.preview=null;}this.p=p;const list=this.$('sources-selected');list.replaceChildren();this.$('sources-open').textContent=tr_web_prephase_resources_js('Joindre des fichiers · ')+(p.sources?.length||0)+'/8';for(const source of p.sources||[]){const item=document.createElement('li'),text=document.createElement('span'),remove=document.createElement('button');text.textContent=source.path+tr_web_prephase_resources_js(' · lignes ')+source.start_line+'–'+source.end_line+tr_web_prephase_resources_js(' · instantané du ')+new Date(source.captured_at).toLocaleString();remove.type='button';remove.textContent=tr_web_prephase_resources_js('Retirer cet extrait');remove.addEventListener('click',e=>{if(e.isTrusted)this.mutate('source-remove',{source:{path:source.path}},'sources')});const details=document.createElement('details'),summary=document.createElement('summary'),pre=document.createElement('pre');summary.textContent=tr_web_prephase_resources_js('Lire l’instantané transmis');pre.textContent=source.text;details.append(summary,pre);item.append(text,details,remove);list.append(item)}}
 error(kind,text){this.$(kind+'-error').textContent=text;}
 async open(kind){if(this.hasPending()){this.$((this.pending?.kind||kind)+'-dialog').showModal();return;}try{const p=await this.current();if(!p)throw new Error(tr_web_prephase_resources_js('Enregistrez les documents avant de continuer.'));this.paint(p);this.error(kind,'');this.$(kind+'-dialog').showModal();if(kind==='sources'){await this.search()}else{const v=await this.api('preparations/budget?id='+encodeURIComponent(p.id));this.budgetView(v);const b=v.budget;for(const [id,key] of [['budget-limit','limit_usd'],['budget-reserve','reserve_per_launch_usd'],['budget-date','reference_date'],['budget-source','estimate_source']])this.$(id).value=b[key]??'';}}catch(e){this.error(kind,e.message)}}
 budgetView(v){const usd=n=>Number(n).toLocaleString((globalThis.SwarmI18n?.locale || 'fr-FR'),{minimumFractionDigits:2,maximumFractionDigits:4})+' USD';this.$('budget-summary').textContent=(v.budget.limit_usd>0?tr_web_prephase_resources_js('Limite ')+usd(v.budget.limit_usd)+tr_web_prephase_resources_js(' · disponible ')+usd(v.remaining_usd):tr_web_prephase_resources_js('Aucune limite estimative configurée'))+tr_web_prephase_resources_js(' · réservé ')+usd(v.reserved_usd)+tr_web_prephase_resources_js(' · engagé estimé ')+usd(v.committed_estimate_usd)+'.';this.$('budget-usage').textContent=tr_web_prephase_resources_js('Coût rapporté : ')+(v.actual_cost_usd===null?'inconnu':usd(v.actual_cost_usd)+tr_web_prephase_resources_js(' sur ')+v.cost_turns+'/'+v.turns+tr_web_prephase_resources_js(' échanges'))+'. Jetons rapportés : '+v.input_tokens+tr_web_prephase_resources_js(' entrants, ')+v.output_tokens+tr_web_prephase_resources_js(' sortants, sur ')+v.usage_turns+'/'+v.turns+tr_web_prephase_resources_js(' échanges.');this.$('budget-policy').textContent=v.policy;this.$('budget-work').textContent=v.work_budget?.budget.limit_usd>0?tr_web_prephase_resources_js('Budget partagé du travail : ')+usd(v.work_budget.remaining_usd)+tr_web_prephase_resources_js(' disponibles. Les agents, l’assistant et cette préparation consomment la même enveloppe.'):'';}
 async search(){const generation=++this.generation;this.error('sources','');this.$('source-results').textContent='Recherche…';try{const v=await this.api('preparations/files?'+new URLSearchParams({id:this.p.id,q:this.$('source-query').value,scope:this.$('source-scope').value}));if(generation!==this.generation)return;const dirs=this.$('source-directories');dirs.replaceChildren();for(const path of ['.',...(v.directories||[])]){if(path===this.$('source-scope').value)continue;const b=document.createElement('button');b.type='button';b.textContent=path==='.'?tr_web_prephase_resources_js('Revenir à la racine'):tr_web_prephase_resources_js('Dossier : ')+path;b.addEventListener('click',e=>{if(e.isTrusted){this.$('source-scope').value=path;this.search()}});dirs.append(b)}const list=this.$('source-results');list.replaceChildren();for(const path of v.paths){const item=document.createElement('li'),b=document.createElement('button');b.type='button';b.textContent=path;b.addEventListener('click',e=>{if(e.isTrusted){this.$('source-path').value=path;this.preview=null;this.read()}});item.append(b);list.append(item)}this.$('source-list-status').textContent=v.limited?tr_web_prephase_resources_js('Parcours limité : choisissez un dossier plus précis, puis relancez la recherche.'):v.paths.length+tr_web_prephase_resources_js(' fichiers proposés.');}catch(e){if(generation===this.generation){this.$('source-results').replaceChildren();this.error('sources',e.message)}}}
 async read(){if(this.hasPending())return;this.preview=null;this.$('source-add').disabled=true;this.$('source-preview').textContent='Lecture…';const generation=++this.generation;try{const v=await this.api('preparations/source?'+new URLSearchParams({id:this.p.id,path:this.$('source-path').value,start:this.$('source-start').value,end:this.$('source-end').value}));if(generation!==this.generation)return;this.preview=v;this.$('source-preview').textContent=v.text;this.$('source-preview-label').textContent=v.path+tr_web_prephase_resources_js(' · lignes ')+v.start_line+'–'+v.end_line;this.$('source-add').disabled=false;this.error('sources','');}catch(e){if(generation===this.generation){this.$('source-preview').textContent='';this.error('sources',e.message)}}}
 async add(){if(this.pending)return this.mutate(null,null,'sources');if(!this.preview)return;const {path,start_line,end_line,sha256}=this.preview;return this.mutate('source-add',{source:{path,start_line,end_line,sha256}},'sources');}
 async saveBudget(){const b={limit_usd:Number(this.$('budget-limit').value),reserve_per_launch_usd:Number(this.$('budget-reserve').value),reference_date:this.$('budget-date').value,estimate_source:this.$('budget-source').value};await this.mutate('budget',{budget:b},'budget');}
 async mutate(action,extra,kind){if(this.busy)return;this.busy=true;try{let pending=this.pending;if(!pending){const p=await this.current();if(!p)throw new Error(tr_web_prephase_resources_js('Enregistrez les documents avant de continuer.'));pending={kind,request:{version:1,action,preparation_id:p.id,expected_revision:p.revision,event_id:crypto.randomUUID(),...extra}};this.pending=pending;}await this.apply(pending.request);this.pending=null;this.error(kind,kind==='sources'?tr_web_prephase_resources_js('Extraits enregistrés. Relisez et adoptez le brief avant de demander un plan.'):tr_web_prephase_resources_js('Budget enregistré.'));if(kind==='budget')this.budgetView(await this.api('preparations/budget?id='+encodeURIComponent(this.p.id)));}catch(e){if(e.status&&e.status<500)this.pending=null;this.error(kind,e.message+(this.pending?tr_web_prephase_resources_js(' Réessayez : le même événement vérifiera l’enregistrement sans le doubler.'):''));}finally{this.busy=false;this.$('source-add').textContent=this.pending?tr_web_prephase_resources_js('Vérifier l’enregistrement'):tr_web_prephase_resources_js('Joindre cet extrait');this.$('source-add').disabled=!this.pending&&!this.preview;this.$('budget-save').textContent=this.pending?tr_web_prephase_resources_js('Vérifier l’enregistrement'):tr_web_prephase_resources_js('Enregistrer le budget');}}
}
